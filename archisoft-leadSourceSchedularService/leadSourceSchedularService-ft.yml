server:
  port: 8672

spring:
  profiles:
    active: dev
  cloud:
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: true

management:
  security:
    enable: false

archisoft:
  scheduler:
    retriveLeadData:
      cron-expression: 0 0/15 * * * ?
  dates:
    duplicatedateperiod: 14
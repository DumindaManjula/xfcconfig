server:
  port: 8685

spring:
  profiles:
    active: ft
  data:
    mongodb:
      uri: ${DB_URL:mongodb://192.168.0.207:27017}
      database: mtx_buyer
  cloud:
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: true

management:
  security:
    enable: false